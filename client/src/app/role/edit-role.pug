login-toolbar(title='permission.title')
include ../permission/toolbar

.container
	side-bar

	.maincolumn
		.title-bar
			.title {{ 'permission.updatingRole' | translate }}
		hr

		include role-form

	.bottom-bar
		button.btn.btn-ok(ng-click='saveRole()') Save
		button.btn.btn-cancel(ng-click='cancel()') Cancel