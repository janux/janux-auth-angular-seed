.section-form
	p.form-section-glarus
		| {{ section | translate}}
	div.an-material
		md-input-container.full
			label {{ 'staff.job' | translate}}
			input(ng-model='data.job.title')

		md-input-container.full
			label {{ 'staff.area' | translate}}
			input(ng-model='data.job.area')