doctype html
html(ng-app='MyApp')
	head
		title Janux Authorization
		meta(charset='utf-8')
		meta(name='viewport', content='width=device-width, initial-scale=1'')

		base(href='/')
		link(rel='stylesheet', href=cfg.server.static+'/'+cfg.dir.css+'/main.css?'+cfg.build.timestamp)
		link(rel='stylesheet', href=cfg.server.static+'/'+cfg.dir.css+'/angular-aside.min.css')

		script(src=cfg.server.static+'/'+cfg.file.app+'?'+cfg.build.timestamp)

	body
		#top
		section(ui-view)

// vim: set fo=rq wrap:
